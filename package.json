{
  "name": "programacion-practica",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "node --test tests/*.test.mjs",
    "validate:bootstrap": "node scripts/validate-bootstrap.mjs",
    "check": "npm run validate:bootstrap && npm run validate:content && npm run validate:study && npm run validate:quiz && npm run lint && npm run typecheck && npm test && npm run build",
    "validate:content": "node scripts/validate-content.mjs",
    "validate:study": "node scripts/validate-study.mjs",
    "validate:quiz": "node scripts/validate-quiz.mjs"
  },
  "engines": {
    "node": ">=22.13.0"
  },
  "dependencies": {
    "next": "16.3.5",
    "react": "19.3.0",
    "react-dom": "19.3.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "4.3.3",
    "@types/node": "22.20.4",
    "@types/react": "19.3.0",
    "@types/react-dom": "19.3.0",
    "eslint": "9.39.5",
    "eslint-config-next": "16.3.5",
    "tailwindcss": "4.3.3",
    "typescript": "6.0.3"
  }
}
